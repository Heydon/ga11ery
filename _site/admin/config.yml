backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
publish_mode: editorial_workflow
media_folder: '_site/images'
public_folder: '/images'
collections:
  - name: 'posts'
    label: 'Posts'
    folder: 'src/posts'
    create: true
    slug: '{{slug}}'
    fields:
      - {label: 'Title', name: 'title', widget: 'string'}
      - {label: 'Image', name: 'image', widget: 'image'}
      - {label: 'Alternative text', name: 'alt', widget: 'string', hint: 'Describe the image for the blind. What can you see in it? What does it represent?'}
      - {label: 'Full description', name: 'description', widget: 'text', required: false}
      - {label: 'Publish date', name: 'date', widget: 'datetime'}